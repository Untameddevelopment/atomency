{
  "name": "atomency",
  "version": "1.1.0",
  "description": "Interactive chemistry education web application with periodic table, molecular builder, and lab simulations",
  "private": true,
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'Static site — no build step required'"
  }
}
